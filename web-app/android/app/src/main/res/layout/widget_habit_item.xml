<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/widget_habit_item_bg"
    android:paddingStart="16dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:layout_marginBottom="6dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Color Overlay (tinted background - set programmatically) -->
    <FrameLayout
        android:id="@+id/habit_color_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.09" />

    <!-- Left: Trait Icon Container -->
    <FrameLayout
        android:id="@+id/habit_icon_container"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/widget_icon_container_bg">

        <TextView
            android:id="@+id/habit_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textSize="18sp"
            android:text="🎯" />

    </FrameLayout>

    <!-- Center: Title + Progress -->
    <LinearLayout
        android:id="@+id/habit_text_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/habit_icon_container"
        android:layout_toStartOf="@id/habit_action_container"
        android:layout_centerVertical="true"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="8dp">

        <!-- Title Row: Name + Streak -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/habit_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.01"
                android:maxLines="1"
                android:ellipsize="end"
                android:text="Habit Title" />

            <!-- Streak Badge -->
            <LinearLayout
                android:id="@+id/habit_streak_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="@drawable/widget_streak_badge_bg"
                android:paddingStart="5dp"
                android:paddingEnd="5dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:layout_marginStart="6dp"
                android:visibility="gone">

                <TextView
                    android:id="@+id/habit_streak_icon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🔥"
                    android:textSize="9sp"
                    android:layout_marginEnd="2dp" />

                <TextView
                    android:id="@+id/habit_streak_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#fb923c"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:fontFamily="sans-serif-medium" />

            </LinearLayout>

        </LinearLayout>

        <!-- Progress Text -->
        <TextView
            android:id="@+id/habit_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.02"
            android:layout_marginTop="2dp"
            android:text="0/1" />

        <!-- Subtasks Container (for CHECKLIST type) -->
        <LinearLayout
            android:id="@+id/habit_subtasks_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Right: Complete Button (Progress Circle) -->
    <LinearLayout
        android:id="@+id/habit_action_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Progress Circle / Complete Button -->
        <FrameLayout
            android:id="@+id/habit_complete_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/widget_progress_circle"
            android:clickable="true"
            android:focusable="true">

            <!-- Check Icon (shown when completed) -->
            <TextView
                android:id="@+id/habit_check_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="✓"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold"
                android:visibility="gone" />

        </FrameLayout>

    </LinearLayout>

</RelativeLayout>
